# Required for SQL generation and explanations. Never commit a real key.
OPENAI_API_KEY=

# Application defaults
NQL_MODEL=gpt-4o-mini
NQL_DB_PATH=naturalql.duckdb
NQL_RESULT_LIMIT=50
NQL_TODAY=2025-09-10

# Validation bounds for model-generated SQL
NQL_MAX_SQL_LENGTH=20000
NQL_MAX_AST_NODES=500
